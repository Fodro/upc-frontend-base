import { CSSObject } from '@emotion/css';

export const styles: Record<string, CSSObject> = {
	common: {
		fontFamily: 'Montserrat',
		fontStyle: 'normal',
		fontWeight: 800,
		fontSize: '3rem',
		textAlign: 'center',
	},
	sub: {
		fontWeight: 600,
		fontSize: '1.5rem',
	},
};
